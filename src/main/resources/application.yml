server:
  port: 8080

spring:
  application:
    name: smartTransportation
  main:
    allow-bean-definition-overriding: true
  # 禁用DashScope自动配置
  autoconfigure:
    exclude:
      - com.alibaba.cloud.ai.autoconfigure.dashscope.DashScopeAutoConfiguration
  ai:
    # DashScope 配置已切换到 ideaLAB OpenAI 兼容接口
    # dashscope:
    #   api-key: sk-550ab25272734c96b9570a862903b482
    #   chat:
    #     options:
    #       model: qwen-plus
    openai:
        api-key: 96662af9a5fa753462e291e3a219cd89
        base-url: https://idealab.alibaba-inc.com/api/openai
        chat:
          options:
            model: qwen3-coder-plus
            temperature: 0.7


  # MySQL 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/smart_transportation?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: Qwert_1245
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  # Redis 配置
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

# Milvus 配置 (已启用，连接本地Milvus服务)
milvus:
  enabled: true  # 启用 Milvus 向量数据库
  host: localhost
  port: 19530
  token: ""  # 本地模式不需要token
  database: default
  connection:
    timeout: 30000
    keep-alive-time: 30000
    keep-alive-timeout: 20000
    max-idle-timeout: 60000
    retry-times: 3

# 天气接口配置（用于曼哈顿 2024 年 2 月默认天气数据）
weather:
  api:
    enabled: true
    base-url: https://weather.visualcrossing.com/VisualCrossingWebServices/rest/services/timeline
    key: 324LPPF8896FXKECQWWZQEEKU   # 请在环境变量或此处填入接口 Key
    location: Manhattan,NY
    start-date: 2024-02-01
    end-date: 2024-02-29

tavily:
  api-key: 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
